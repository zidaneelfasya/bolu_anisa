import { PergerakanStokClient } from "./pergerakan-stok-client";

export default function PergerakanStokPage() {
  return (
    <div className="w-full h-full p-2">
      <PergerakanStokClient />
    </div>
  );
}
